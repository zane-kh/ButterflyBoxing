import React from "react";
import { Helmet } from "react-helmet";

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name="description" content={description} />
      <meta name="keyword" content={keywords} />
    </Helmet>
  );
};

Meta.defaultProps = {
  title: "Welcome To Butterfly Boxing",
  description: "We sell the best equipment for cheap",
  keywords: "combat gear, buy combat gear, cheap combat gear",
};

export default Meta;
